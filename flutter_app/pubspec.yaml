name: sourcing_manager_os
description: Trustless real estate operating system for India.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  google_fonts: ^8.1.0
  intl: ^0.20.2
  http: ^1.1.0
  geolocator: ^14.0.2
  image_picker: ^1.1.2
  hive_flutter: ^1.1.0
  image: ^4.5.4
  cryptography: ^2.7.0
  uuid: ^4.3.3
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
